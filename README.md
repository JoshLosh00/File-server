A simple http file server written in C using concurrency via poll for multiplexed client handling. 
Error handling is supported but a known limitation is that error messages are not sent asynchronoursly.
